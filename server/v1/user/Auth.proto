syntax = "proto3";

package server.user;

message AuthRequest {
  string email = 1;
  string password = 2;
}

message AuthResponse {
	string accessToken = 1;
	int64 expiresIn = 2;
	string refreshToken= 3;
}

service AuthController {
  rpc Call(AuthRequest) returns (AuthResponse) {}
}